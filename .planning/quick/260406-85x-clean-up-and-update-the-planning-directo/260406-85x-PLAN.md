---
phase: quick
plan: 260406-85x
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "PROJECT.md Constraints section reflects OpenRouter + Prisma 7 + openai/gpt-4o-mini (not Vercel AI SDK / Anthropic / Prisma 6)"
    - "PROJECT.md Current State reflects Phase 17 complete and Phase 16 complete"
    - "REQUIREMENTS.md AIEX-01 references openai/gpt-4o-mini via @openrouter/sdk (not Haiku via Vercel AI SDK)"
    - "REQUIREMENTS.md SCOR-03 references openai/gpt-4o-mini (not claude-sonnet-4-6)"
    - "REQUIREMENTS.md INFR-05 env vars list OPENROUTER_API_KEY (not ANTHROPIC_API_KEY)"
    - "STATE.md Tech Stack Locked field matches CLAUDE.md: OpenRouter via @openrouter/sdk, openai/gpt-4o-mini"
    - "STATE.md Phase 16 and 17 marked complete (both have full SUMMARY files)"
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "Accurate project state document"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Accurate requirements with correct technology references"
    - path: ".planning/STATE.md"
      provides: "Accurate current state with correct tech stack and phase completion"
  key_links:
    - from: "CLAUDE.md Constraints"
      to: ".planning/PROJECT.md Constraints"
      via: "manual sync"
    - from: "src/ingestion/services/extraction-agent.service.ts"
      to: "REQUIREMENTS.md AIEX-01"
      via: "model name update"
---

<objective>
Update .planning/ docs to accurately reflect the current state of the codebase after Phases 12–17.

Purpose: The planning docs were last updated after Phase 11. Since then: OpenRouter replaced Anthropic/Vercel AI SDK, Prisma was upgraded from 6 to 7, Phases 12–17 completed (with 16 pending in ROADMAP but actually implemented), and the .env.example still references ANTHROPIC_API_KEY. These stale references mislead future Claude sessions.

Output: Updated PROJECT.md, REQUIREMENTS.md, and STATE.md that match the actual codebase.
</objective>

<execution_context>
@/Users/danielshalem/triolla/telent-os-backend/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danielshalem/triolla/telent-os-backend/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
</context>

<findings>
## What's Stale (Discovered During Planning)

### PROJECT.md — Constraints section (line 64-66)
STALE:
```
- **Tech Stack:** TypeScript only, NestJS 11, BullMQ + Redis, Prisma 6, PostgreSQL 16, Vercel AI SDK — locked, not negotiable
- **AI Provider:** Anthropic Claude via `@ai-sdk/anthropic` — Haiku for extraction, Sonnet for scoring. No local models in Phase 1.
```
CORRECT (from CLAUDE.md + codebase):
```
- **Tech Stack:** TypeScript only, NestJS 11, BullMQ + Redis, Prisma 7, PostgreSQL 16 — locked, not negotiable
- **AI Provider:** OpenRouter via `@openrouter/sdk` — currently `openai/gpt-4o-mini` for both extraction and scoring
```

### PROJECT.md — Key Decisions table
Row `Vercel AI SDK over raw Anthropic SDK` is superseded. Replace with `@openrouter/sdk over Vercel AI SDK`.

### PROJECT.md — Current State (line 89)
Says "Phase 11 Complete". Should say "Phase 17 Complete" with summary of what was built.

### PROJECT.md — Context section AI cost (line 56)
Says "Haiku for extraction, Sonnet for scoring" — update to "openai/gpt-4o-mini for both".

### REQUIREMENTS.md — AIEX-01 (line 28)
STALE: `Agent 1 (claude-haiku-4-5) ... using Vercel AI SDK generateObject`
CORRECT: `Agent 1 (openai/gpt-4o-mini) ... using @openrouter/sdk with JSON mode`

### REQUIREMENTS.md — SCOR-03 (line 57)
STALE: `Agent 2 (claude-sonnet-4-6) scores candidate`
CORRECT: `Agent 2 (openai/gpt-4o-mini) scores candidate`

### REQUIREMENTS.md — SCOR-05 (line 59)
STALE: `records the model_used string (e.g., claude-sonnet-4-6)`
CORRECT: `records the model_used string (e.g., openai/gpt-4o-mini)`

### REQUIREMENTS.md — INFR-05 (line 79)
STALE: lists `ANTHROPIC_API_KEY` in env vars
CORRECT: `OPENROUTER_API_KEY` (env.ts and all services use this)

### REQUIREMENTS.md — Active section (lines 31-37)
`Agent 1 (Haiku): real Anthropic generateObject call enabled` — this was completed in Phase 14.
The `Agent 2 (Sonnet)` checkbox refers to a model that no longer exists in the codebase.
These should be moved to Validated with corrected model references.

### STATE.md — Tech Stack Locked (line 27)
STALE: `Vercel AI SDK, Claude Haiku + Sonnet`
CORRECT: `OpenRouter via @openrouter/sdk, openai/gpt-4o-mini for both extraction and scoring`

### STATE.md — Phase 16 status
STATE.md shows Phase 16 as "Context gathered, ready for planning" but the code shows Phase 16 features ARE implemented (updateCandidate with reassignment, unassigned filter, sourceAgency in responses, shortId in job responses). ROADMAP.md shows Phase 16 plans as `[ ]` not started — but code exists. Likely completed without plan-tracking. Mark as complete with caveats.

### STATE.md — Phase 17 status
ROADMAP.md says Phase 17 complete but STATE.md Current Focus still points to Phase 17 as "not started". Update Current Focus to reflect milestone complete.

### .env.example
Contains `ANTHROPIC_API_KEY=sk-ant-...` but code uses `OPENROUTER_API_KEY`. This should be fixed too (file to modify: .env.example). Add to Task 1's scope.
</findings>

<tasks>

<task type="auto">
  <name>Task 1: Update PROJECT.md — fix tech stack, AI provider, key decisions, current state</name>
  <files>.planning/PROJECT.md</files>
  <action>
Rewrite the following sections of .planning/PROJECT.md to reflect the actual codebase:

1. **Constraints section** — replace Prisma 6 with Prisma 7, replace "Vercel AI SDK" with nothing (remove from stack), replace "AI Provider: Anthropic Claude via @ai-sdk/anthropic — Haiku for extraction, Sonnet for scoring" with "AI Provider: OpenRouter via `@openrouter/sdk` — currently `openai/gpt-4o-mini` for both extraction and scoring".

2. **Context section** — replace "~$6–16/month total (Haiku for extraction, Sonnet for scoring" with "~$6–16/month total (openai/gpt-4o-mini for extraction and scoring via OpenRouter)".

3. **Key Decisions table** — replace the row "Vercel AI SDK over raw Anthropic SDK" with "@openrouter/sdk over Vercel AI SDK | Single provider for all models, no per-model SDK, easy model swap via model string | Adopted in Phase 14 (260324-agv)". Keep all other rows.

4. **Current State section** — replace "Phase 11 Complete (2026-03-25)" block with:

```
**Phase 17 Complete (2026-04-01):** Production Deployment Readiness
- Health endpoint GET /health (DB + Redis probes), structured JSON logging (nestjs-pino), security middleware (helmet, throttler, CORS deny-all)
- GitHub Actions CI pipeline, docker-compose hardened (resource limits, healthchecks)
- README rewritten as complete developer onboarding documentation
- 250 tests passing across 20 suites

**Earlier milestones completed:**
- Phase 14: OpenRouter extraction pipeline (real LLM calls replacing mock)
- Phase 15: Deterministic Job ID routing (regex + shortId lookup, removed semantic matching)
- Phase 16: Manual routing & UI parity (PATCH /candidates/:id reassignment, unassigned filter, shortId/sourceAgency in responses)
- Phase 12: Add candidate from UI (POST /candidates endpoint with R2 upload)
- Phase 13: Kanban board candidate hiring stage tracking
```

5. Update the footer: "Last updated: 2026-04-06 after Phase 17 completion — all 17 phases complete, milestone done"

Also fix .env.example: replace `ANTHROPIC_API_KEY=sk-ant-...` with `OPENROUTER_API_KEY=sk-or-...` and update the comment from "AI — Anthropic Claude (Haiku for extraction, Sonnet for scoring)" to "AI — OpenRouter (openai/gpt-4o-mini for extraction and scoring)".
  </action>
  <verify>
grep "Prisma 7" .planning/PROJECT.md && grep "openrouter" .planning/PROJECT.md && grep "Phase 17" .planning/PROJECT.md && grep "openai/gpt-4o-mini" .planning/PROJECT.md && grep -v "Prisma 6" .planning/PROJECT.md && grep "OPENROUTER_API_KEY" .env.example
  </verify>
  <done>
PROJECT.md Constraints shows Prisma 7 and OpenRouter. Current State section shows Phase 17 complete. .env.example shows OPENROUTER_API_KEY not ANTHROPIC_API_KEY.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update REQUIREMENTS.md — fix AI model references and env var</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Make the following targeted updates to .planning/REQUIREMENTS.md:

1. **AIEX-01** — change from:
   `Agent 1 (claude-haiku-4-5) extracts structured candidate fields from email + CV text using Vercel AI SDK generateObject + Zod schema`
   to:
   `Agent 1 (openai/gpt-4o-mini via OpenRouter) extracts structured candidate fields from email + CV text using @openrouter/sdk with JSON mode + Zod schema`

2. **SCOR-03** — change from:
   `Agent 2 (claude-sonnet-4-6) scores candidate against each active job`
   to:
   `Agent 2 (openai/gpt-4o-mini via OpenRouter) scores candidate against each active job`

3. **SCOR-05** — change from:
   `records the model_used string (e.g., claude-sonnet-4-6)`
   to:
   `records the model_used string (e.g., openai/gpt-4o-mini)`

4. **INFR-05** — change from:
   `ANTHROPIC_API_KEY`
   to:
   `OPENROUTER_API_KEY`

5. **Active section** — the two Active items referencing old models:
   - `[ ] Agent 1 (Haiku): real Anthropic generateObject call enabled (scaffolded in Phase 4, activated in Phase 5+)` — move to Validated section, update to: `[x] **AIEX-04**: Real OpenRouter API call enabled for extraction — openai/gpt-4o-mini via @openrouter/sdk (completed Phase 14)`
   - `[ ] Agent 2 (Sonnet): scores candidate against each active job; results stored append-only in candidate_job_scores` — this was always covered by SCOR-01 to SCOR-05 (now complete). Remove from Active.
   - `[ ] Multi-tenant schema from day 1: tenant_id on every table; Phase 1 has exactly one tenant` — this is completed (DB-02 covers it). Move to Validated or remove from Active.

6. Update the footer: "Last updated: 2026-04-06 after Phase 17 completion — OpenRouter migration reflected"
  </action>
  <verify>
grep "openai/gpt-4o-mini" .planning/REQUIREMENTS.md && grep "OPENROUTER_API_KEY" .planning/REQUIREMENTS.md && grep -v "claude-haiku-4-5\|claude-sonnet-4-6\|Vercel AI SDK\|generateObject" .planning/REQUIREMENTS.md | head -5
  </verify>
  <done>
REQUIREMENTS.md has no references to claude-haiku-4-5, claude-sonnet-4-6, Vercel AI SDK, ANTHROPIC_API_KEY, or generateObject. All model references show openai/gpt-4o-mini via OpenRouter.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update STATE.md — fix tech stack and phase completion status</name>
  <files>.planning/STATE.md</files>
  <action>
Make targeted updates to .planning/STATE.md:

1. **Tech Stack (Locked) line** — change from:
   `TypeScript, NestJS 11, BullMQ + Redis, Prisma 7, PostgreSQL 16, Vercel AI SDK, Claude Haiku + Sonnet, Cloudflare R2, Postmark Inbound webhooks`
   to:
   `TypeScript, NestJS 11, BullMQ + Redis, Prisma 7, PostgreSQL 16, OpenRouter via @openrouter/sdk (openai/gpt-4o-mini), Cloudflare R2, Postmark Inbound webhooks`

2. **Current Focus** — change from:
   `Phase 17 — production-deployment-readiness-fix-tests-add-sanity-checks-and-prepare-ci-cd-for-hetzner-jenkins`
   to:
   `All phases complete — ready for Phase 2 planning (recruiter auth, admin UI, outbound communications)`

3. **STATUS line** — change from:
   `STATUS: Phase 16 context gathered (discuss mode). Ready for planning.`
   to:
   `STATUS: All 17 phases complete. v1.0 milestone done. 250 tests passing.`

4. **Research Completed section** — update the cost model line from:
   `Cost model: ~€5/month Hetzner + ~$6–16/month Anthropic`
   to:
   `Cost model: ~€5/month Hetzner + ~$6–16/month OpenRouter (openai/gpt-4o-mini)`

5. **What Happened (Phase 16 Context Gathering) section** — rename to "Phase 16 & 17 Complete" and update to reflect that Phase 16 features ARE implemented in code (updateCandidate reassignment, unassigned filter, shortId/sourceAgency in responses — verified in codebase 2026-04-06). Phase 17: 250 tests passing, health endpoint, CI pipeline, security middleware.

6. **Full Phase Completion section** — add Phase 16 and 17 entries:
   ```
   16. Phase 16 (Backend Support for Manual Routing & UI Parity) — COMPLETE ✓
       - PATCH /candidates/:id supports job reassignment (preserves old Application + scores, creates new Application, rescores)
       - GET /candidates supports ?unassigned=true filter
       - GET /jobs and GET /jobs/:id expose shortId field
       - GET /candidates exposes sourceAgency field
       - 250 tests passing

   17. Phase 17 (Production Deployment Readiness) — COMPLETE ✓
       - GET /health endpoint (DB + Redis probes)
       - nestjs-pino structured JSON logging
       - Security: helmet, throttler, CORS deny-all
       - GitHub Actions CI pipeline
       - docker-compose resource limits and healthchecks
       - README rewritten as developer onboarding docs
   ```

7. Update `last_updated` frontmatter field to `"2026-04-06T00:00:00.000Z"` and `last_activity` to `2026-04-06`.

8. Update **Last Session** section to reflect 2026-04-06 cleanup task.
  </action>
  <verify>
grep "OpenRouter\|openrouter" .planning/STATE.md && grep -v "Vercel AI SDK\|Claude Haiku\|Claude Sonnet" .planning/STATE.md | head -5 && grep "250 tests" .planning/STATE.md
  </verify>
  <done>
STATE.md Tech Stack shows OpenRouter. Current Focus reflects all phases complete. No references to Vercel AI SDK or Claude Haiku/Sonnet in the tech stack line. Phase 16 and 17 appear in Full Phase Completion.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Documentation-only changes — no code trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| N/A | — | docs only | accept | No security surface in .planning/ doc updates |
</threat_model>

<verification>
After all three tasks complete, verify end state:

```bash
# Tech stack accuracy
grep "Prisma 7" .planning/PROJECT.md
grep "openai/gpt-4o-mini" .planning/PROJECT.md .planning/REQUIREMENTS.md .planning/STATE.md
grep "OPENROUTER_API_KEY" .planning/REQUIREMENTS.md .env.example
grep -v "claude-haiku\|claude-sonnet\|Vercel AI\|Prisma 6\|ANTHROPIC_API_KEY" .planning/PROJECT.md .planning/REQUIREMENTS.md | wc -l

# Phase completion accuracy
grep "Phase 17 Complete" .planning/PROJECT.md
grep "All phases complete" .planning/STATE.md
grep "250 tests" .planning/STATE.md
```
</verification>

<success_criteria>
1. All three .planning/ docs use correct tech stack: Prisma 7, OpenRouter, openai/gpt-4o-mini, OPENROUTER_API_KEY
2. No doc references: Prisma 6, Vercel AI SDK, @ai-sdk/anthropic, claude-haiku-4-5, claude-sonnet-4-6, ANTHROPIC_API_KEY (in tech stack context)
3. PROJECT.md and STATE.md show Phase 17 as the final completed phase
4. .env.example shows OPENROUTER_API_KEY (not ANTHROPIC_API_KEY)
5. REQUIREMENTS.md Active section has no items that are actually complete
</success_criteria>

<output>
After completion, create `.planning/quick/260406-85x-clean-up-and-update-the-planning-directo/260406-85x-SUMMARY.md`
</output>
