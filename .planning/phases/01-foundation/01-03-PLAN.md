---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - Dockerfile
  - docker-compose.yml
  - .env.example
autonomous: false
requirements:
  - INFR-04
  - INFR-05
  - PROC-01

user_setup:
  - service: docker
    why: "Docker Desktop or Docker Engine must be running to test docker-compose up"
    env_vars: []
    dashboard_config:
      - task: "Ensure Docker is running"
        location: "Docker Desktop app or systemctl start docker on Linux"

must_haves:
  truths:
    - "docker-compose up starts all 4 services: api, worker, postgres, redis"
    - "api and worker are separate containers (different processes, per PROC-01)"
    - "postgres and redis have health checks; api and worker wait for them via depends_on condition: service_healthy"
    - ".env.example documents all 10 required env vars with correct keys"
    - "Dockerfile is multi-stage: builder stage compiles TypeScript, runner stage runs dist/"
    - "Worker container starts with command: node dist/worker.js (not dist/main.js)"
  artifacts:
    - path: "Dockerfile"
      provides: "Multi-stage build: builder (tsc) + runner (node dist/main.js)"
      contains: "FROM node:22-alpine AS builder"
    - path: "docker-compose.yml"
      provides: "4-service orchestration with health checks"
      contains: "condition: service_healthy"
    - path: ".env.example"
      provides: "All required env vars documented"
      contains: "DATABASE_URL"
  key_links:
    - from: "docker-compose.yml worker service"
      to: "Dockerfile"
      via: "command: node dist/worker.js override"
      pattern: "node dist/worker.js"
    - from: "docker-compose.yml api/worker"
      to: "postgres/redis"
      via: "depends_on condition: service_healthy"
      pattern: "condition: service_healthy"
---

<objective>
Create the multi-stage Dockerfile, docker-compose.yml with 4 services and health checks, and the `.env.example` file documenting all required environment variables.

Purpose: Local development and production deployment share identical configuration. Docker Compose is the only way to run the full stack end-to-end — without it, Phase 2 webhook testing is impossible.
Output: `docker-compose up --wait` starts all 4 services healthy; `.env.example` acts as onboarding documentation for every new environment.
</objective>

<execution_context>
@/Users/danielshalem/triolla/telent-os-backend/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danielshalem/triolla/telent-os-backend/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

<interfaces>
<!-- Authoritative reference: spec/backend-architecture-proposal.md §10 -->

Base docker-compose from spec §10 (to be extended with health checks per D-06):
```yaml
services:
  api:
    build: .
    ports: ['3000:3000']
    env_file: .env
    depends_on: [postgres, redis]

  worker:
    build: .
    command: node dist/worker.js    # D-05: same Dockerfile, different command
    env_file: .env
    depends_on: [postgres, redis]

  postgres:
    image: postgres:16-alpine
    volumes: ['postgres_data:/var/lib/postgresql/data']
    environment:
      POSTGRES_DB: triolla
      POSTGRES_USER: triolla
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}

  redis:
    image: redis:7-alpine
    volumes: ['redis_data:/data']
```

D-06 addition: add `healthcheck` to postgres and redis; update `depends_on` in api and worker to use `condition: service_healthy`.

Env vars (INFR-05, spec §10):
  DATABASE_URL, REDIS_URL, ANTHROPIC_API_KEY, POSTMARK_WEBHOOK_TOKEN,
  TENANT_ID, R2_ACCOUNT_ID, R2_ACCESS_KEY_ID, R2_SECRET_ACCESS_KEY,
  R2_BUCKET_NAME, NODE_ENV (10 total)

D-04: TENANT_ID pre-filled with 00000000-0000-0000-0000-000000000001 in .env.example

Dockerfile strategy (Claude's discretion — multi-stage preferred):
  Stage 1 (builder): node:22-alpine, install all deps, copy src/, run nest build
  Stage 2 (runner): node:22-alpine, install prod deps only, copy dist/ from builder
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create multi-stage Dockerfile and docker-compose.yml with health checks</name>
  <read_first>
    - spec/backend-architecture-proposal.md §10 (base docker-compose YAML and env vars list)
    - .planning/phases/01-foundation/01-CONTEXT.md §decisions (D-05, D-06)
    - package.json (to confirm build script is "nest build" and start:prod is "node dist/main")
  </read_first>
  <files>Dockerfile, docker-compose.yml</files>
  <action>
    **1. Create Dockerfile** — multi-stage build (Claude's discretion: multi-stage for smaller prod image):

    ```dockerfile
    # ─── Stage 1: Build ───────────────────────────────────────────────────────────
    FROM node:22-alpine AS builder

    WORKDIR /app

    # Copy dependency manifests first for layer caching
    COPY package*.json ./
    RUN npm ci

    # Copy source and compile TypeScript
    COPY . .
    RUN npm run build

    # ─── Stage 2: Production runner ───────────────────────────────────────────────
    FROM node:22-alpine AS runner

    WORKDIR /app

    # Install production deps only (smaller image)
    COPY package*.json ./
    RUN npm ci --omit=dev

    # Copy compiled output from builder stage
    COPY --from=builder /app/dist ./dist

    # Copy Prisma schema for migrations (prisma generate needs schema at runtime)
    COPY --from=builder /app/node_modules/.prisma ./node_modules/.prisma
    COPY --from=builder /app/node_modules/@prisma ./node_modules/@prisma
    COPY prisma ./prisma

    EXPOSE 3000

    # Default command starts the API server
    # Worker overrides this in docker-compose.yml with: command: node dist/worker.js
    CMD ["node", "dist/main.js"]
    ```

    **2. Create docker-compose.yml** — base from spec §10, extended with D-06 health checks:

    ```yaml
    services:
      api:
        build: .
        ports:
          - '3000:3000'
        env_file: .env
        depends_on:
          postgres:
            condition: service_healthy
          redis:
            condition: service_healthy
        restart: unless-stopped

      worker:
        build: .
        command: node dist/worker.js
        env_file: .env
        depends_on:
          postgres:
            condition: service_healthy
          redis:
            condition: service_healthy
        restart: unless-stopped

      postgres:
        image: postgres:16-alpine
        volumes:
          - postgres_data:/var/lib/postgresql/data
        environment:
          POSTGRES_DB: triolla
          POSTGRES_USER: triolla
          POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
        healthcheck:
          test: ['CMD-SHELL', 'pg_isready -U triolla -d triolla']
          interval: 5s
          timeout: 5s
          retries: 10
          start_period: 10s

      redis:
        image: redis:7-alpine
        volumes:
          - redis_data:/data
        healthcheck:
          test: ['CMD', 'redis-cli', 'ping']
          interval: 5s
          timeout: 5s
          retries: 10
          start_period: 5s

    volumes:
      postgres_data:
      redis_data:
    ```
  </action>
  <verify>
    <automated>docker-compose config 2>&1 | grep -E "^services|worker|api|postgres|redis" | head -10 && echo "compose config valid"</automated>
  </verify>
  <acceptance_criteria>
    - `docker-compose config` exits 0 (valid YAML, no syntax errors)
    - `grep "condition: service_healthy" docker-compose.yml | wc -l` returns 4 (both api and worker depend on both postgres and redis with health condition)
    - `grep "node dist/worker.js" docker-compose.yml` matches exactly 1 line
    - `grep "FROM node:22-alpine AS builder" Dockerfile` matches 1 line
    - `grep "FROM node:22-alpine AS runner" Dockerfile` matches 1 line
    - `grep "npm ci --omit=dev" Dockerfile` matches 1 line (prod deps only in runner stage)
    - `grep "CMD.*dist/main.js" Dockerfile` matches 1 line (default command is API)
    - `grep "healthcheck" docker-compose.yml | wc -l` returns at least 4 (postgres + redis each have healthcheck block)
    - Dockerfile does NOT contain `npm run start:dev` (no dev server in Docker)
  </acceptance_criteria>
  <done>Dockerfile and docker-compose.yml created; docker-compose config validates without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Create .env.example with all required env vars</name>
  <read_first>
    - spec/backend-architecture-proposal.md §10 §Environment Variables (exact var names and example values)
    - .planning/REQUIREMENTS.md §Infrastructure INFR-05 (full list of 10 required vars)
    - .planning/phases/01-foundation/01-CONTEXT.md D-04 (TENANT_ID pre-filled value)
  </read_first>
  <files>.env.example</files>
  <action>
    Create `.env.example` with all 10 required environment variables. Comments explain each var's purpose. TENANT_ID is pre-filled per D-04:

    ```bash
    # Database — PostgreSQL 16 (matches docker-compose.yml postgres service)
    DATABASE_URL=postgresql://triolla:password@postgres:5432/triolla

    # Queue — Redis 7 (matches docker-compose.yml redis service)
    REDIS_URL=redis://redis:6379

    # AI — Anthropic Claude (Haiku for extraction, Sonnet for scoring)
    ANTHROPIC_API_KEY=sk-ant-...

    # Email intake — Postmark HMAC-SHA256 signature verification token
    POSTMARK_WEBHOOK_TOKEN=...

    # Multi-tenancy — hardcoded dev tenant UUID (Phase 1 has exactly one tenant)
    TENANT_ID=00000000-0000-0000-0000-000000000001

    # Cloudflare R2 object storage (S3-compatible) — stores original CV files
    R2_ACCOUNT_ID=...
    R2_ACCESS_KEY_ID=...
    R2_SECRET_ACCESS_KEY=...
    R2_BUCKET_NAME=triolla-cvs

    # Runtime environment
    NODE_ENV=production

    # PostgreSQL superuser password (for docker-compose postgres service)
    POSTGRES_PASSWORD=changeme
    ```
  </action>
  <verify>
    <automated>grep -c "^[A-Z]" .env.example</automated>
  </verify>
  <acceptance_criteria>
    - `.env.example` exists and is non-empty
    - `grep "DATABASE_URL=" .env.example` matches 1 line
    - `grep "REDIS_URL=" .env.example` matches 1 line
    - `grep "ANTHROPIC_API_KEY=" .env.example` matches 1 line
    - `grep "POSTMARK_WEBHOOK_TOKEN=" .env.example` matches 1 line
    - `grep "TENANT_ID=00000000-0000-0000-0000-000000000001" .env.example` matches 1 line (pre-filled per D-04)
    - `grep "R2_ACCOUNT_ID=" .env.example` matches 1 line
    - `grep "R2_ACCESS_KEY_ID=" .env.example` matches 1 line
    - `grep "R2_SECRET_ACCESS_KEY=" .env.example` matches 1 line
    - `grep "R2_BUCKET_NAME=" .env.example` matches 1 line
    - `grep "NODE_ENV=" .env.example` matches 1 line
    - `.env.example` does NOT contain any real secrets (all values are placeholders or example strings)
  </acceptance_criteria>
  <done>.env.example contains all 10 required vars; TENANT_ID pre-filled with hardcoded dev UUID.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify Docker Compose starts all 4 services healthy</name>
  <action>Human verification of Docker orchestration — run docker-compose up and confirm all services reach healthy state.</action>
  <what-built>
    Docker Compose orchestration:
    - Dockerfile: multi-stage build (builder compiles TS, runner uses dist/)
    - docker-compose.yml: api, worker, postgres (16-alpine), redis (7-alpine) with health checks
    - .env.example: all 10 required vars documented, TENANT_ID pre-filled
    - Worker container overrides command to `node dist/worker.js` (separate from api)
    - postgres and redis have health checks; api/worker depend on them with `condition: service_healthy`
  </what-built>
  <how-to-verify>
    1. Copy .env.example to .env and fill in POSTGRES_PASSWORD=changeme (leave others as placeholders)
    2. Run: `docker-compose build --no-cache` — must succeed without errors
    3. Run: `docker-compose up --wait` — all 4 services must reach healthy/running state
    4. Run: `docker-compose ps` — confirm both `api` and `worker` containers are listed separately (PROC-01)
    5. Run: `docker-compose exec postgres pg_isready -U triolla -d triolla` — must return "accepting connections"
    6. Run: `docker-compose exec redis redis-cli ping` — must return "PONG"
    7. Run: `docker-compose exec api wget -qO- http://localhost:3000` — may return 404 (no routes yet) but must NOT timeout (confirms API is up)
    8. Run: `docker-compose logs worker --tail=5` — confirm worker started without crash (even if "Connection refused" for Redis is OK at this stage — what matters is the process started)
    9. Run: `docker-compose down`
  </how-to-verify>
  <resume-signal>Type "approved" if all 4 services started healthy. Describe any issues if not.</resume-signal>
</task>

</tasks>

<verification>
Automated checks before checkpoint:
1. `docker-compose config` exits 0 — YAML syntax valid
2. `grep "condition: service_healthy" docker-compose.yml | wc -l` returns 4
3. `grep "node dist/worker.js" docker-compose.yml` returns 1 match
4. `grep "TENANT_ID=00000000-0000-0000-0000-000000000001" .env.example` matches

Manual checkpoint verifies actual Docker orchestration works end-to-end.
</verification>

<success_criteria>
- docker-compose build succeeds
- docker-compose up --wait starts all 4 services healthy
- api and worker are separate containers (PROC-01)
- postgres health check passes (pg_isready)
- redis health check passes (ping → PONG)
- .env.example has all 10 vars; TENANT_ID pre-filled
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
