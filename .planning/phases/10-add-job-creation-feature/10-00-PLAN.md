---
phase: 10-add-job-creation-feature
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - src/jobs/jobs.service.spec.ts
  - src/jobs/jobs.controller.spec.ts
  - src/jobs/jobs.integration.spec.ts
autonomous: true
requirements: [D-04, D-05, D-06, D-07, D-08, D-01, D-02]

must_haves:
  truths:
    - "Three test stub files exist under src/jobs/ and Jest can discover them"
    - "Running `npm test -- --testPathPattern=jobs` exits 0 (all stubs are it.todo, no assertions to fail)"
    - "Each stub file imports the correct module-under-test path (even though module doesn't exist yet)"
  artifacts:
    - path: "src/jobs/jobs.service.spec.ts"
      provides: "Unit test stubs for createJob(), default stage seeding, tenant isolation"
      contains: "it.todo"
    - path: "src/jobs/jobs.controller.spec.ts"
      provides: "Controller test stubs for POST /jobs, Zod validation errors"
      contains: "it.todo"
    - path: "src/jobs/jobs.integration.spec.ts"
      provides: "Integration test stubs for backward compat (Application.stage, old Job fields)"
      contains: "it.todo"
  key_links:
    - from: "src/jobs/jobs.service.spec.ts"
      to: "src/jobs/jobs.service.ts"
      via: "import"
      pattern: "from.*jobs.service"
---

<objective>
Create three failing test stub files under src/jobs/ so that every behavior targeted in Phase 10 has a named placeholder test before any implementation is written.

Purpose: Nyquist compliance — every requirement has a test home before implementation starts. Prevents silent gaps where code ships without test coverage.
Output: Three .spec.ts files with it.todo() entries. Jest runs them at 0 failures.
</objective>

<execution_context>
@/Users/danielshalem/triolla/telent-os-backend/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danielshalem/triolla/telent-os-backend/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-add-job-creation-feature/10-CONTEXT.md
@.planning/phases/10-add-job-creation-feature/10-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create jobs.service.spec.ts stub</name>
  <files>src/jobs/jobs.service.spec.ts</files>
  <read_first>
    - src/jobs/jobs.service.ts (existing service — import path to use)
    - src/jobs/jobs.module.ts (module structure for TestingModule)
  </read_first>
  <action>
Create `src/jobs/jobs.service.spec.ts` with a Jest describe block importing `JobsService` from `./jobs.service`. Use a minimal TestingModule with `JobsService`, a jest-mocked `PrismaService` (mock object with `job: { create: jest.fn() }`), and a jest-mocked `ConfigService` (mock object with `get: jest.fn().mockReturnValue('test-tenant-id')`). Do NOT use `@nestjs/testing` createTestingModule if it requires resolving PrismaModule — use direct instantiation: `new JobsService(mockPrisma as any, mockConfig as any)`.

Include these it.todo stubs (exactly these strings):

```
describe('JobsService', () => {
  describe('createJob()', () => {
    it.todo('D-04: auto-seeds 4 default stages when hiringStages is omitted');
    it.todo('D-04: default stages are Application Review, Screening, Interview, Offer in order');
    it.todo('D-05: auto-seeded stages have isCustom=false');
    it.todo('D-07: uses provided hiringStages when supplied in dto');
    it.todo('D-06: creates screeningQuestions from dto.screeningQuestions');
    it.todo('D-06: assigns tenantId from ConfigService to all nested creates');
    it.todo('D-09: responsibleUserId on JobStage is free text string, not UUID-validated');
  });
});
```

The file must compile without errors even before `createJob()` method exists — wrap test body imports in a factory function or use jest.mock() if needed. The simplest path: stub the module with `beforeEach(() => { service = new JobsService(mockPrisma as any, mockConfig as any); })` and leave all it.todo bodies empty.
  </action>
  <verify>
    <automated>cd /Users/danielshalem/triolla/telent-os-backend && npm test -- --testPathPattern=jobs.service 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `src/jobs/jobs.service.spec.ts`
    - File contains: `it.todo('D-04: auto-seeds 4 default stages`
    - File contains: `it.todo('D-05: auto-seeded stages have isCustom=false`
    - File contains: `it.todo('D-07: uses provided hiringStages when supplied in dto`
    - File contains: `from './jobs.service'`
    - `npm test -- --testPathPattern=jobs.service` exits 0 with "0 failed" or "todo" in output
  </acceptance_criteria>
  <done>7 it.todo stubs exist in jobs.service.spec.ts, Jest discovers and passes the file with 0 failures</done>
</task>

<task type="auto">
  <name>Task 2: Create jobs.controller.spec.ts stub</name>
  <files>src/jobs/jobs.controller.spec.ts</files>
  <read_first>
    - src/jobs/jobs.controller.ts (existing controller — import path, existing @Get decorator)
    - src/jobs/jobs.service.ts (service interface to mock)
  </read_first>
  <action>
Create `src/jobs/jobs.controller.spec.ts`. Use direct instantiation: `new JobsController(mockJobsService as any)` where `mockJobsService = { findAll: jest.fn(), createJob: jest.fn() }`.

Include these it.todo stubs:

```
describe('JobsController', () => {
  describe('POST /jobs', () => {
    it.todo('D-06: calls jobsService.createJob with validated dto');
    it.todo('D-08: returns 400 when title is missing');
    it.todo('D-08: returns 400 when answerType is invalid enum value');
    it.todo('D-08: returns 201 with created job on valid payload');
  });
});
```
  </action>
  <verify>
    <automated>cd /Users/danielshalem/triolla/telent-os-backend && npm test -- --testPathPattern=jobs.controller 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `src/jobs/jobs.controller.spec.ts`
    - File contains: `it.todo('D-06: calls jobsService.createJob`
    - File contains: `it.todo('D-08: returns 400 when title is missing`
    - File contains: `from './jobs.controller'`
    - `npm test -- --testPathPattern=jobs.controller` exits 0
  </acceptance_criteria>
  <done>4 it.todo stubs exist in jobs.controller.spec.ts, Jest discovers and passes with 0 failures</done>
</task>

<task type="auto">
  <name>Task 3: Create jobs.integration.spec.ts stub</name>
  <files>src/jobs/jobs.integration.spec.ts</files>
  <read_first>
    - src/applications/applications.service.ts (line 58: `stage: a.stage` — the backward-compat field this test must guard)
  </read_first>
  <action>
Create `src/jobs/jobs.integration.spec.ts`. This file guards backward compatibility. It does NOT need a real DB connection — use mocked services. Direct instantiation pattern same as above.

Include these it.todo stubs:

```
describe('Phase 10 integration', () => {
  describe('backward compatibility', () => {
    it.todo('D-01: Job.description field still exists and is readable after migration');
    it.todo('D-01: Job.requirements[] still exists and is readable after migration');
    it.todo('D-02: Application.stage field still returned by ApplicationsService.findAll()');
    it.todo('D-02: Application.jobStageId is nullable and does not break findAll() on existing applications');
    it.todo('D-03: ScoringAgentService is not imported or called from JobsService');
  });
  describe('POST /jobs end-to-end', () => {
    it.todo('D-06: job created with hiringStages and screeningQuestions in single operation');
    it.todo('D-07: job created with default stages when hiringStages omitted');
  });
});
```
  </action>
  <verify>
    <automated>cd /Users/danielshalem/triolla/telent-os-backend && npm test -- --testPathPattern=jobs.integration 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `src/jobs/jobs.integration.spec.ts`
    - File contains: `it.todo('D-02: Application.stage field still returned`
    - File contains: `it.todo('D-06: job created with hiringStages`
    - `npm test -- --testPathPattern=jobs.integration` exits 0
  </acceptance_criteria>
  <done>7 it.todo stubs exist in jobs.integration.spec.ts, Jest discovers and passes with 0 failures</done>
</task>

</tasks>

<verification>
Run full jobs test suite: `npm test -- --testPathPattern=jobs`
Expected: 0 failures, all tests show as "todo", 3 test suites discovered.
</verification>

<success_criteria>
- 3 spec files exist under src/jobs/
- `npm test -- --testPathPattern=jobs` exits 0
- Every it.todo string references the decision ID (D-04, D-05, etc.) it will verify
- No implementation code in these files — stubs only
</success_criteria>

<output>
After completion, create `.planning/phases/10-add-job-creation-feature/10-00-SUMMARY.md`
</output>
